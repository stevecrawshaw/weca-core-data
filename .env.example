# WECA Core Data ETL - Environment Variables
# Copy this file to .env and fill in your credentials
# DO NOT commit .env to version control (it's in .gitignore)

# =============================================================================
# EPC API Configuration
# =============================================================================
# Get your API key from: https://epc.opendatacommunities.org/
# The api_key should be base64 encoded: base64(email:apikey)
#
# If you already have credentials in ../config.yml, use the 'auth_token' value
SOURCES__EPC__API_KEY=your_base64_encoded_token_here

# =============================================================================
# NOMIS API Configuration (Optional)
# =============================================================================
# NOMIS API credentials for employment data
# Usually available in ../config.yml
# NOMIS__USERNAME=your_nomis_username
# NOMIS__PASSWORD=your_nomis_password

# =============================================================================
# Pipeline Configuration
# =============================================================================
# Control which data sources to download (true/false)
# DOWNLOAD_EPC=true
# DOWNLOAD_LSOA=true
# DOWNLOAD_POSTCODES=true

# =============================================================================
# How to generate EPC API key:
# =============================================================================
# import base64
# email = "your.email@example.com"
# api_key = "your_api_key_from_epc_portal"
# token = base64.b64encode(f"{email}:{api_key}".encode()).decode()
# print(f"SOURCES__EPC__API_KEY={token}")
